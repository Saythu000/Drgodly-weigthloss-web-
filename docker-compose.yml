version: '3.8'

services:
  drgodly-web:
    image: ghcr.io/saythu000/drgodly-web:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: drgodly-telehealth-app
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOSTNAME=0.0.0.0
      - DATABASE_URL=postgresql://neondb_owner:npg_eq5cxn4dpfFB@ep-soft-mode-at8wk6v3-pooler.c-9.us-east-1.aws.neon.tech/neondb?sslmode=require
      - NEXTAUTH_SECRET=drgodly_telehealth_crm_secret_key_2026
      - NEXTAUTH_URL=http://localhost:3000
    volumes:
      - openclaw_data:/app/.openclaw-local

volumes:
  openclaw_data:
